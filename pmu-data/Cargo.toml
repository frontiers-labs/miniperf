[package]
name = "miniperf-pmu-data"
version = "0.1.1"
edition = "2021"
rust-version = "1.85"
description = "Serializable CPU PMU event-table schema used by miniperf"
license = "MIT OR LGPL-3.0-only"
repository = "https://github.com/frontiers-labs/miniperf"
homepage = "https://github.com/frontiers-labs/miniperf"
readme = "README.md"
keywords = ["performance", "profiling", "pmu", "schema"]
categories = ["development-tools::profiling", "data-structures"]
include = ["src/**", "README.md", "CHANGELOG.md"]

[lib]
name = "pmu_data"

[dependencies]
serde = { version = "1.0.217", features = ["derive"] }

[package.metadata.docs.rs]
rustdoc-args = ["--cfg", "docsrs"]
