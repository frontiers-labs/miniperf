# Coverage comes from checks/, which drives the packaged binaries on real
# recordings. See checks/lint/test-policy.
[package]
name = "miniperf-shims"
version = "0.1.0"
edition = "2024"

[lib]
test = false
doctest = false
name = "shims"

[dev-dependencies]
libc = "0.2"
miniperf-store = { path = "../store" }
tempfile = "3"
