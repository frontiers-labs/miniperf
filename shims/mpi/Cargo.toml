# Coverage comes from checks/, which drives the packaged binaries on real
# recordings. See checks/lint/test-policy.
[package]
name = "miniperf-shim-mpi"
version = "0.1.1"
edition = "2024"

[lib]
test = false
doctest = false
name = "mperf_mpi"
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"
