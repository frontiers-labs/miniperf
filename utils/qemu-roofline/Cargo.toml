# Coverage comes from checks/, which drives the packaged binaries on real
# recordings. See checks/lint/test-policy.
[package]
name = "miniperf-qemu-roofline"
version = "0.1.1"
edition = "2021"

[lib]
test = false
doctest = false
name = "miniperf_qemu_roofline"
crate-type = ["cdylib"]

[dependencies]
miniperf-roofline-core = { path = "../roofline-core" }
libc = "0.2"
