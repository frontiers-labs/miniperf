[package]
name = "miniperf-roofline-core"
version = "0.1.1"
edition = "2021"

[lib]
name = "roofline_core"
crate-type = ["lib", "staticlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
serde_json = "1.0"
